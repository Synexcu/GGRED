---
import BaseLayout from "@/layouts/BaseLayout.astro"
import { Button, buttonVariants } from "@/components/ui/button"
import MaxWidthWrapper from "@/components/MaxWidthWrapper.astro";
import { FaDiscord } from "react-icons/fa";

import { fetchClanInfo } from '@@/clanReq.tsx';

let GGRED = await fetchClanInfo(1);
let GGBLU = await fetchClanInfo(2);
---

<BaseLayout
  title="Welcome to GGRED Website!"
  description="See our community, Check out our achievements, or just looking around? We got'chu bro, come on in."
>
  <section class="grid items-center gap-6 pb-8"> <!-- pt-6 pb-8 -->
    <div class="flex bg-[url('/bg-ggred.jpg')] min-w-full min-h-screen bg-cover bg-no-repeat items-center">
      <div class="bg-gradient-to-r from-gray-950 to-transparent min-w-full">
        <MaxWidthWrapper className="min-h-screen flex min-[320px]:pt-10">
          <div class="w-full h-[calc(100vh-5rem)] pt-20 flex flex-col justify-center gap-4 px-4 md:w-[50%]">
            <div class="text-5xl text-white font-bold md:text-6xl">Welcome to <a class="text-red-500">{GGRED?.tag}</a> </div>
            <div class="text-md text-white md:text-base min-[500px]:w-[70%]">
              <a class="font-bold">Outwit, Outplay, Outlast – Dive into the Thrill of Strategic Naval Warfare!</a> <br/><br/>
              Welcome to our World of Warships Community!
            </div>
            <div>
              <a href="https://discord.gg/z43eSF5uMH" target="_blank" rel="noreferrer" class={buttonVariants({ variant: 'redDiscord' })}>
                <FaDiscord className="mr-2"/>
                Join our Discord!!
              </a>
            </div>
          </div>
        </MaxWidthWrapper>
      </div>
    </div>
  </section>
  
  <section class="container grid items-center gap-6 pb-8 pt-6 md:py-10">
    <h1
    class="text-3xl font-extrabold leading-tight tracking-tighter text-center md:text-4xl"
    >
    Welcome to {GGRED?.tag}
    </h1>
    <p>
      {GGRED ? (
        <div>
            <h2>{GGRED?.name}</h2>
            <p>Tag: {GGRED?.tag}</p>
            <p>Leader: {GGRED?.leader_name}</p>
            <p>Members: {GGRED?.members_count}</p>
            <h3>Members</h3>
            <ul>
            </ul>
        </div>
    ) : (
        <p>Failed to load clan data.</p>
    )}
    </p>
  </section>

  <!-- <section class="container grid items-center gap-6 pb-8 pt-6 md:py-10">
    <div class="flex max-w-[980px] flex-col items-start gap-2">
      <h1
        class="text-3xl font-extrabold leading-tight tracking-tighter md:text-4xl"
      >
        Astro Shadcn UI Template <br class="hidden sm:inline" />
        built with Radix UI and Tailwind CSS.
      </h1>
      <p class="max-w-[700px] text-lg text-muted-foreground">
        Accessible and customizable components that you can copy and paste into
        your apps. Free. Open Source.
      </p>
    </div>
    <div class="flex gap-4">
      <a
        href="https://ui.shadcn.com"
        target="_blank"
        rel="noreferrer"
        class={buttonVariants()}
      >
        Documentation
      </a>
      <a
        href="https://github.com/AREA44/astro-shadcn-ui-template"
        target="_blank"
        rel="noreferrer"
        class={buttonVariants({ variant: 'outline' })}
      >
        GitHub
      </a>
    </div>
  </section> -->
</BaseLayout>
